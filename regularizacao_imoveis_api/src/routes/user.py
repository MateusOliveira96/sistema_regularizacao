# Este arquivo foi substituído por users.py e auth.py
# Mantido apenas para compatibilidade com o template

from flask import Blueprint

user_bp = Blueprint('user', __name__)

# Rotas movidas para users.py e auth.py
